name = "bartonic"
compatibility_date = "2025-03-01"
main = "worker.js"
workers_dev = true

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
# Run the Worker only for the BART proxy; everything else is static SPA assets.
run_worker_first = ["/api/*"]
